Conway is an implementation of Conway's Game of Life.

Requirements
------------
+ Python 3
+ pygame

Testing
-------

Tests are performed using the unittest package. To perform a test one can simple run the proper command in the main directory.

Examples:

python -m unittests tests\test_tilemap.py
python -m unittests discover -s tests